Side notes:
Added getter for debugMode, rows, columns, and flags in Minefield.java.
Added setter for debugMode.
If player tries placing flag on a already placed flag area it will just return true and cannot remove a already placed flag

How to Play:
Run Main.java in the terminal and begin playing. Can choose from Easy, Medium, and Hard difficulty.
